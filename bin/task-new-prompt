#!/bin/bash
# Called from zellij keybinding to create and start a task (no AI)
# Runs in a floating pane that closes on exit
printf "New task: "
read -r input
if [ -n "$input" ]; then
    escaped=$(printf '%s' "$input" | sed "s/'/\\\\'/g")
    fish -c "task new '$escaped' && task start --last"
fi
