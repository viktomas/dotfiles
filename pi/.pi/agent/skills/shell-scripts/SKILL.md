---
name: shell-scripts
description: ALWAYS use this skill before writing shell scripts that will be persisted. You don't have to use this if you are writing inline throw-away scirpts.
---

Use `jq` for JSON parsing.

Use `shellcheck` to check the final script.
